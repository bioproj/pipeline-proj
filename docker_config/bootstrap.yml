spring:
  application:
    name:  service-pipeline
  # cloud 配置
  cloud:
    nacos:
      discovery:
        # 注册中心地址
        server-addr: ${NACOS_HOST}:28848
        group: DEV_GROUP
      config:
        file-extension: yml
        # 配置中心地址
        server-addr: ${NACOS_HOST}:28848
        group: DEV_GROUP